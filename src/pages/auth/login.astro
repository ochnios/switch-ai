---
import Layout from "@/layouts/Layout.astro";
import { LoginForm } from "@/components/auth/LoginForm";

// Authentication check is handled by middleware
// Authenticated users are automatically redirected to /app/conversations/new
// See src/middleware/index.ts for route protection logic
---

<Layout title="Sign in - switch-ai">
  <LoginForm client:load />
</Layout>
