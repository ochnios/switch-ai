---
import Layout from "@/layouts/Layout.astro";
import { RegisterForm } from "@/components/auth/RegisterForm";

// TODO: Add SSR check - if user already authenticated, redirect to /app/conversations/new or last conversation
// This will be implemented when middleware and auth system are ready
---

<Layout title="Sign up - switch-ai">
  <RegisterForm client:load />
</Layout>
